<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <style name="AppBaseTheme.NoTitle" parent="android:Theme.Light">
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme.NoTitle" parent="AppBaseTheme">
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- Progreess Bar. -->
    <style name="Widget"></style>

    <style name="Widget.ProgressBar">
        <item name="android:indeterminateOnly">true</item>
        <item name="android:indeterminateBehavior">repeat</item>
        <item name="android:indeterminateDuration">3500</item>
        <item name="android:minWidth">48dip</item>
        <item name="android:maxWidth">48dip</item>
        <item name="android:minHeight">48dip</item>
        <item name="android:maxHeight">48dip</item>
    </style>

    <style name="Widget.ProgressBar.MiniProgressBar">
        <item name="android:indeterminateOnly">false</item>
        <item name="android:progressDrawable">@drawable/miniprogressbar</item>
        <item name="android:indeterminateDrawable">@android:drawable/progress_indeterminate_horizontal</item>
        <item name="android:minHeight">1dip</item>
        <item name="android:maxHeight">10dip</item>
    </style>

    <style name="Text" parent="@android:style/TextAppearance" />
    <style name="Text.Level" parent="@style/Text">
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#328597</item>
        <item name="android:gravity">center</item>
    </style>
    <style name="Text.Level.Big" parent="@style/Text.Level">
        <item name="android:textSize">26.0dip</item>
        <item name="android:background">@drawable/bg_level</item>
        <item name="android:paddingBottom">@dimen/gapSmall</item>
    </style>

    <style name="Button" parent="@android:style/Widget.Button" />
    <style name="Button.Green" parent="@style/Button">
        <item name="android:textSize">15.0dip</item>
        <item name="android:textColor">#ffffffff</item>
        <item name="android:background">@drawable/sel_btn_green</item>
    </style>
    <style name="Button.Green.Big" parent="@style/Button.Green">
        <item name="android:textSize">30.0dip</item>
        <item name="android:paddingLeft">30.0dip</item>
        <item name="android:paddingRight">30.0dip</item>
    </style>
</resources>